# SYSU2025 机器学习大作业：Generative Agents for Recommendation（基于 Agent4Rec）

本项目为 **SYSU 2025 年机器学习课程大作业**，基于论文 **“On Generative Agents in Recommendation”**

（SIGIR 2024）提出的开源项目 Agent4Rec，并进行了一系列改进与扩展。 

\- 原始项目（Agent4Rec）：https://github.com/LehengTHU/Agent4Rec  

\- 本项目：在原始 Agent4Rec 的基础上进行修改与增强，用于课程实验与分析。

## 1. 项目简介

推荐系统研究中，离线指标（如 Recall/NDCG）与线上真实交互效果往往存在差距。论文 **“On Generative Agents in Recommendation”** 提出使用 **LLM 驱动的生成式用户代理（Generative Agents）** 来模拟更接近真实用户的多轮浏览与反馈行为，从而为推荐算法提供一种更“类人”的交互式评测环境。

本项目复现并使用 Agent4Rec 作为交互评测器（Interaction Evaluator），在多页推荐交互场景下输出如下指标（示例）：
- Average recall / Average precision
- Overall click rate
- Maximum exit page / Average exit page
- Average number of likes
等

---

## 2. 我们的改动（相对原始 Agent4Rec）

### ✅ 2.1 可配置的大语言模型接口（核心改动）
原始代码在用户代理（user agent）侧默认**固定使用 GPT-3.5**，这会带来：
- 模型选择受限（难以对比不同 LLM 的表现）
- 不利于课程实验与消融分析（例如比较不同 LLM 或不同 API 服务）

为此我们设计并实现了 **可配置的大语言模型（LLM）接口**：
- 将原先“写死的模型调用”抽象为统一接口
- 支持通过配置/参数指定使用不同 LLM
- 实验中我们采用 **Deepseek3.1** 作为用户代理模型，完成仿真实验与结果分析

> 注：如果你需要在实验中切换 LLM（如 OpenAI / Deepseek / 其他兼容服务），只需调整配置项即可，无需改动核心仿真流程。

---

## 3. 环境与依赖

> 下面给出一份通用安装方式。若你的仓库中已有 `requirements.txt` / `environment.yml`，请以实际文件为准。

### 3.1 创建环境（推荐）
```bash
conda create -n agent4rec python=3.9 -y
conda activate agent4rec
pip install -r requirements.txt
```

## 🗂 仓库结构

```text
Agent4Rec/
├── assets/                 # 项目相关图片、说明性资源
├── datasets/               # 数据集及预处理脚本（如 MovieLens）
├── recommenders/           # 推荐算法实现与相关模块
├── scripts/                # 实验脚本、批量运行工具
├── simulation/             # 用户代理与交互仿真核心逻辑
├── storage/                # 仿真输出结果（日志、轨迹等，已被 git 忽略）
├── .gitignore              # Git 忽略规则
├── main.py                 # 项目主入口，启动仿真流程
├── parse.py                # 参数解析与配置相关工具
├── requirements.txt        # Python 依赖列表
├── requirements.noreckit.txt # 去除 Recbole/RecKit 依赖的简化版本
├── Original_README.md      # 原始 Agent4Rec 项目的 READMEAgent4Rec/
├── datasets/                  # 数据预处理脚本与原始数据
├── recommenders/              # 不同推荐算法实现
├── simulation/                # 模拟器与代理行为逻辑
├── storage/                  # 模拟运行的输出（被 .gitignore 忽略）
├── config/                    # 模型与实验参数配置文件
├── main.py                    # 项目入口脚本
├── requirements.txt           # Python 环境依赖
└── README.md                  # 项目说明（本文件）
```